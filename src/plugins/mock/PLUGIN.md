# Mock Plugin

Test plugin for OWL. Emits realistic email and calendar style events on an interval so the daemon, event processor, discovery engine, and channels can be exercised without live credentials.
